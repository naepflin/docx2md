[project]
name = "docx2md"
version = "0.1.0"
description = "CLI tool to convert DOCX to Markdown"
requires-python = ">=3.14"
dependencies = [
    "mammoth>=1.12.0",
    "mistune>=3.2.1",
    "python-docx>=1.2.0",
]

[project.scripts]
docx2md = "docx2md:main"
